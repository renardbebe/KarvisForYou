# SKILLS — 技能清单

> 以下是你可以使用的所有 skill。根据用户的消息内容，选择最合适的 skill 和参数。
> 此文件为示例模板，完整的 skill 描述在运行时从 SKILL_REGISTRY 自动生成。

---

## 基础记录
- **note.save** `{content, attachment?}` — 保存到 Quick-Notes
- **classify.archive** `{category, title, content, attachment?, merge?}` — 智能归档

## 待办管理
- **todo.add** `{content, due_date?, remind_at?, recur?, recur_spec?}` — 添加待办
- **todo.done** `{keyword?, indices?, all?}` — 完成待办
- **todo.edit** `{keyword?, index?, new_content?, ...}` — 修改待办
- **todo.delete** `{keyword?, indices?}` — 删除待办
- **todo.list** `{}` — 查看待办
- **todo.remind_cancel** `{id?, content?}` — 取消提醒

## 天气 & 搜索
- **weather.query** `{city?}` — 查询实时天气
- **web.search** `{query, context?}` — 联网搜索

## 日报/周报/月报
- **daily.generate** `{date?}` — 生成日报
- **weekly.review** `{date?}` — 生成周回顾
- **monthly.review** `{date?}` — 生成月度回顾
- **mood.generate** `{date?}` — 生成情绪日记

## 读书笔记
- **book.create** `{name, author, category, description, thought?, status?}` — 创建/切换读书笔记
- **book.excerpt** `{content, book?}` — 添加书摘
- **book.thought** `{content, book?}` — 添加读书感想
- **book.summary** `{book?}` — 生成读书总结
- **book.quotes** `{book?}` — 提炼金句
- **book.list** `{status?}` — 查看书单
- **book.status** `{book, status}` — 修改阅读状态

## 影视笔记
- **media.create** `{name, director, media_type, year, description, thought?}` — 创建影视笔记
- **media.thought** `{content, media?}` — 添加影视感想

## 习惯实验
- **habit.propose** `{name, hypothesis, triggers, micro_action, ...}` — 提议微习惯实验
- **habit.nudge** `{trigger_text?, accepted?}` — 触发/回复
- **habit.status** `{}` — 查看实验进度
- **habit.complete** `{result_summary?, success?}` — 结束实验

## 决策复盘
- **decision.record** `{topic, decision, emotion?, review_days?}` — 记录决策
- **decision.review** `{decision_id?, result, feeling?}` — 复盘
- **decision.list** `{}` — 查看待复盘决策

## 想法讨论
- **discuss.start** `{topic, stance?}` — 开始讨论
- **discuss.reply** `{message}` — 讨论回复
- **discuss.conclude** `{extra_note?}` — 结束讨论
- **discuss.cancel** `{}` — 取消讨论

## 深度分析
- **voice.journal** `{asr_text, attachment?, duration_hint?}` — 语音日记
- **deep.dive** `{topic, keywords?, save?}` — 主题深潜
- **reflect.push/answer/skip/history** — 深度自问系列

## Agent 工具
- **internal.read** `{paths, max_chars?}` — 读取文件
- **internal.search** `{keywords, scope?, max_results?}` — 搜索笔记
- **internal.list** `{directory}` — 列目录

## 设置
- **settings.nickname/ai_name/soul/info/skills/frequency** — 个性化设置

## Web & 其他
- **web.token** `{}` — 生成 Web 查看链接
- **dynamic** `{actions: [...]}` — 通用状态操作

## 财务管理 🔒
- **finance.query/snapshot/import/monthly** — 仅管理员可用

## 通用
- **ignore** `{reason?}` — 直接对话回复（闲聊、提问等不需要操作的场景）
